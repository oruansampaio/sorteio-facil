import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginBottom: 10,
    padding: 16,
    backgroundColor: '#1F1E25',
    borderRadius: 8,
    alignItems: 'center',
  },
  name: {
    color: '#FFD700',
    fontSize: 18,
    fontWeight: 'bold',
  },
})