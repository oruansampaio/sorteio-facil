import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginBottom: 20,
    padding: 16,
    backgroundColor: '#1F1E25',
    borderRadius: 8,
    alignItems: 'center',
  },
  text: {
    color: '#31CF67',
    fontSize: 18,
    fontWeight: 'bold',
  },
});
